# Snapshots

This directory contains POST-MORTEM database snapshots created by `scripts/teardown.sh`.

Files here are named `POST-MORTEM_<product>_<timestamp>.tar.gz`.

They are git-ignored by default — add the `.tar.gz` to version control manually if you want to archive it.
